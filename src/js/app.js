import{bookcaseCutList,equalShelves,shelvesForItems}from'./math/shelves.js';const control=(form,name)=>form.elements.namedItem(name);const read=(form,name)=>control(form,name).value;const fmt=(value,digits=2)=>new Intl.NumberFormat('es-ES',{maximumFractionDigits:digits}).format(value);const show=(el,html)=>{el.innerHTML=html;el.hidden=false;el.focus();};const prep=form=>{const error=form.querySelector('[data-error]');if(error)error.hidden=true;};const fail=(form,reason)=>{const error=form.querySelector('[data-error]');error.textContent=reason.message;error.hidden=false;error.focus();};const save=value=>{try{localStorage.setItem('em:v1:last-result',JSON.stringify(value));}catch{}};
function renderPreview(result){const preview=document.querySelector('#shelf-preview');if(!preview)return;preview.innerHTML=result.shelves.map(shelf=>`<i style="bottom:${shelf.lowerEdge/result.height*100}%;height:${result.thickness/result.height*100}%"><span>${fmt(shelf.lowerEdge)} cm</span></i>`).join('');preview.hidden=false;}
document.querySelector('#equal-form')?.addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;prep(form);try{const result=equalShelves(Object.fromEntries(['innerHeight','shelfCount','shelfThickness','reserveBottom','reserveTop'].map(name=>[name,read(form,name)])));show(document.querySelector('#equal-result'),`<p class="eyebrow">Abertura libre</p><h2>${fmt(result.opening)} cm por hueco</h2><ol class="marks">${result.shelves.map(s=>`<li><strong>Balda ${s.number}:</strong> canto inferior a ${fmt(s.lowerEdge)} cm; superior a ${fmt(s.topEdge)} cm</li>`).join('')}</ol><p class="note">Todas las marcas parten de la cara superior de la base interior.</p>`);renderPreview(result);save({type:'equal',...result});}catch(reason){fail(form,reason);}});
document.querySelector('#fit-form')?.addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;prep(form);try{const result=shelvesForItems(Object.fromEntries(['innerHeight','itemHeight','clearance','shelfThickness','reserveBottom','reserveTop'].map(name=>[name,read(form,name)])));show(document.querySelector('#fit-result'),`<p class="eyebrow">Máximo geométrico</p><h2>${result.count} baldas · ${result.openings} huecos</h2><p>Cada hueco queda en ${fmt(result.opening)} cm. Tu mínimo solicitado era ${fmt(result.required)} cm (${fmt(result.itemHeight)} cm de objeto + ${fmt(result.clearance)} cm de holgura).</p><a class="button" href="../repartir-baldas/">Abrir reparto de baldas</a>`);save({type:'fit',...result});}catch(reason){fail(form,reason);}});
document.querySelector('#cut-form')?.addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;prep(form);try{const result=bookcaseCutList({outerWidth:read(form,'outerWidth'),outerHeight:read(form,'outerHeight'),depth:read(form,'depth'),panelThickness:read(form,'panelThickness'),shelfCount:read(form,'shelfCount'),setback:read(form,'setback'),includeBack:read(form,'includeBack')==='true',backThickness:read(form,'backThickness')});show(document.querySelector('#cut-result'),`<p class="eyebrow">Despiece calculado</p><h2>${fmt(result.innerWidth)} × ${fmt(result.innerHeight)} cm interiores</h2><div class="table-wrap"><table><thead><tr><th>Pieza</th><th>Uds.</th><th>Medida</th><th>Grosor</th></tr></thead><tbody>${result.parts.map(p=>`<tr><td>${p.name}</td><td>${p.quantity}</td><td>${fmt(p.length)} × ${fmt(p.width)} cm</td><td>${fmt(p.thickness)} cm</td></tr>`).join('')}</tbody></table></div><p>Área de panel sumada: <strong>${fmt(result.panelArea/10000,3)} m²</strong>, antes de pérdidas y cortes.</p><button class="button" type="button" id="download-cutlist">Descargar CSV</button><p class="note">Revisa el esquema de unión, el sentido de veta y el ancho de sierra.</p>`);document.querySelector('#download-cutlist').addEventListener('click',()=>downloadCsv(result));save({type:'cut',...result});}catch(reason){fail(form,reason);}});
function downloadCsv(result){const rows=[['Pieza','Cantidad','Largo cm','Ancho cm','Grosor cm'],...result.parts.map(p=>[p.name,p.quantity,p.length,p.width,p.thickness])];const csv=rows.map(row=>row.join(';')).join('\n');const link=document.createElement('a');link.href=URL.createObjectURL(new Blob([`\ufeff${csv}`],{type:'text/csv'}));link.download='lista-corte-estanteria.csv';link.click();URL.revokeObjectURL(link.href);}
